# Runtime dependencies for TradeEasy backend (Python 3.11.9 compatible)

# Core framework
Flask>=2.2,<2.3  # Keep Flask 2.2.x to align with flask-mongoengine and existing project code
# Werkzeug 3.x drops APIs Flask 2.2 expects; pin to <3 for compatibility.
Werkzeug>=2.2.2,<3
Flask-Cors>=4.0
Flask-Login>=0.6
Flask-Session>=0.5
Flask-Bcrypt>=1.0
flask-socketio>=5.3,<6

# Persistence
flask-mongoengine>=1.0.0  # Requires Flask 2.2+ for JSONEncoder compatibility
mongoengine>=0.27  # Explicit dependency for Python 3.11 compatibility

# Utilities
python-dotenv>=1.0.1
requests>=2.31
websocket-client>=1.6  # required for `import websocket`
pyotp>=2.9
blinker>=1.7
Flask-Limiter>=2.8  # Re-enabled to protect expensive endpoints like /api/search
cachetools>=5.3  # TTL/LRU caches used for search caching
APScheduler>=3.10  # Background task scheduling for daily scrip updates

# Production server (optional locally, required on Linux deploys)
gunicorn>=20.1,<23

# SocketIO dependencies
python-engineio>=4.8
python-socketio>=5.10

# Production WebSocket support
# eventlet is enabled to provide true asynchronous WebSocket transport in production
eventlet>=0.33 

# Alternative async mode for WebSocket support (keep commented unless migrating from eventlet)
# gevent>=23.9    

# Time zone database for ZoneInfo on Windows
tzdata>=2024.1